
describe.skip('CreateHabitCommandHandler', () => {

    describe('when creating a new habit', () => {
        it('should create the habit if all required information is provided and valid', () => {
    
        });

        it('should reject the creation if the habit name already exists for the user', () => {
            
        });

        it('should reject the creation if required fields are missing', () => {
            
        });

        it('should reject the creation if the frequency and rest time make the habit impractical', () => {
            
        });
    });

});
